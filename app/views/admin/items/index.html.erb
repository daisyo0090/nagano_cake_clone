<main class="item_main_index">
  <div class="item_main_index_h3">
    <h3>商品一覧</h3>
  </div>
  <div class="item_main_index_table">
    <table>
      <tr>
        <th>商品ID</th>
        <th>商品名</th>
        <th>税抜価格</th>
        <th>ジャンル</th>
      </tr>
      <tr>
        <% @items.each do |item| %>
        <td><%= item.id %></td>
        <td><%= link_to 'item.name', item_path(:id) %></td>
        <td><%= item.price %></td>
        <td><%= @genres.name %></td>
        <% end %>
      </tr>
    </table>
  </div>
</main>